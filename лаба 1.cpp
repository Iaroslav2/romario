#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
int main() {
	char k = 0;
	char A[] = { 2,3,6,12,43,5,2,56,5,7 };
	for (char i = 0, i ,< 10 , (i++));
	scanf(A[i]);
	if (A[i] > k)
		k == A[i];
	printf(k);
	return 0;
}
